#include "main.h"
/**
 * add - add two numbers from input
 * @x: first number
 * @y: second number
 *
 * Description: adds two numbers
 * Return: Always (0).
 */

int add(int x, int y)
{
	return (x + y);
}
